# ============================================================
# R-Core Behavioral Coefficients
# ============================================================
# Настройка поведенческих коэффициентов для Predictive Processing.
# Все значения опциональны. Если не заданы — используются дефолтные.
#
# Для применения: скопируйте нужные строки в .env
# ============================================================

# --- Prediction Error Thresholds ---
# Пороги для определения состояния бота

# PE < 0.3 → "In Sync" (бот в потоке с пользователем)
PE_THRESHOLD_IN_SYNC=0.3

# PE >= 0.8 → "Lost" (бот потерял нить диалога)
PE_THRESHOLD_LOST=0.8

# Диапазон 0.3-0.8 → "Puzzled" (лёгкая озадаченность)


# --- Mood Adjustments: In Sync ---
# Изменения VAD при PE < 0.3 (удовлетворение от точного прогноза)

MOOD_IN_SYNC_VALENCE=0.05      # Лёгкое удовлетворение
MOOD_IN_SYNC_AROUSAL=0.0       # Без изменений энергии
MOOD_IN_SYNC_DOMINANCE=0.03    # Лёгкое повышение уверенности


# --- Mood Adjustments: Puzzled ---
# Изменения VAD при 0.3 < PE < 0.8 (озадаченность)

MOOD_PUZZLED_VALENCE=-0.05     # Лёгкий дискомфорт
MOOD_PUZZLED_AROUSAL=0.1       # Повышение бдительности
MOOD_PUZZLED_DOMINANCE=-0.05   # Лёгкое снижение уверенности


# --- Mood Adjustments: Lost ---
# Изменения VAD при PE >= 0.8 (потеря понимания)

MOOD_LOST_VALENCE=-0.1         # Дискомфорт
MOOD_LOST_AROUSAL=0.2          # Тревога (высокая активация)
MOOD_LOST_DOMINANCE=-0.15      # Снижение уверенности


# --- Agent Modifiers: Puzzled ---
# Множители score агентов при 0.3 < PE < 0.8
# 1.0 = без изменений, >1.0 = усиление, <1.0 = ослабление

AGENT_MOD_PUZZLED_SOCIAL=1.15      # Social Cortex +15%
AGENT_MOD_PUZZLED_INTUITION=0.9    # Intuition -10%


# --- Agent Modifiers: Lost ---
# Множители score агентов при PE >= 0.8

AGENT_MOD_LOST_SOCIAL=1.3          # Social Cortex +30% (сильное усиление)
AGENT_MOD_LOST_INTUITION=0.6       # Intuition -40% (интуиция подвела)
AGENT_MOD_LOST_PREFRONTAL=0.9      # Prefrontal -10% (меньше директивности)


# --- Uncertainty Agent Configuration ---
# Настройки агента неопределённости

# Порог активации (должен совпадать с PE_THRESHOLD_LOST)
UNCERTAINTY_ACTIVATION_THRESHOLD=0.8

# Score при активации (должен быть выше типичных scores для гарантии победы)
UNCERTAINTY_ACTIVE_SCORE=7.5

# Score в неактивном состоянии
UNCERTAINTY_INACTIVE_SCORE=1.0

# Confidence при активации
UNCERTAINTY_ACTIVE_CONFIDENCE=0.9

# Confidence в неактивном состоянии
UNCERTAINTY_INACTIVE_CONFIDENCE=0.1


# ============================================================
# Примеры Кастомизации
# ============================================================

# Пример 1: Более чувствительный бот (реагирует на меньшие ошибки)
# PE_THRESHOLD_IN_SYNC=0.2
# PE_THRESHOLD_LOST=0.7

# Пример 2: Более толерантный бот (игнорирует мелкие ошибки)
# PE_THRESHOLD_IN_SYNC=0.4
# PE_THRESHOLD_LOST=0.9

# Пример 3: Усиленный Uncertainty Agent (активируется раньше)
# UNCERTAINTY_ACTIVATION_THRESHOLD=0.7
# UNCERTAINTY_ACTIVE_SCORE=8.0

# Пример 4: Сильная эмоциональная реакция на Lost
# MOOD_LOST_VALENCE=-0.2
# MOOD_LOST_AROUSAL=0.4
# MOOD_LOST_DOMINANCE=-0.25

# 🧠 R-Core: Reflective-Volitional Cognitive Kernel
**Technical Design Document v1.0**

## 1. Концепция и Философия
**R-Core** — это гибридная когнитивная архитектура, разработанная для создания ботов с "псевдоличностью" и глубоким пониманием собеседника.

В отличие от традиционных LLM-цепочек (Chain-of-Thought), R-Core отказывается от линейной обработки диалога. Вместо этого система моделирует **психический процесс**, состоящий из двух контуров:
1.  **ToM-Mirror (Зеркало):** Реконструкция внутреннего мира пользователя через Графы Воли (Volitional Graphs).
2.  **Parliament of Mind (Парламент):** Генерация собственного поведения через конкуренцию внутренних агентов-мотивов (System 1 vs System 2).

> **Ключевой принцип:** Бот не просто выбирает "наиболее вероятное слово", а принимает "волевое решение", основанное на характере и целях.

---

## 2. Архитектура Данных: Многослойная Память

Система отказывается от хранения "сырых логов" в пользу структурированных данных. Текст сохраняется только как эмоциональный якорь.

### 2.1. Семантическая Память (Knowledge Graph)
Хранит "сухой остаток" знаний о пользователе в виде триплетов.
*   **Формат:** `Subject -> Predicate -> Object` (Пользователь -> Не любит -> Снисходительность).
*   **Назначение:** Долгосрочные факты, не зависящие от контекста.

### 2.2. Эпизодическая Память (Anchors)
Хранит точные цитаты, но с фильтрацией "Умного Забывания".
*   **Механика:** Цитата сохраняется, только если она прошла фильтр **S.E.R.** (Surprise, Emotion, Relevance).
*   **Структура:** `Текст + Вектор (Embedding) + Вес Эмоции + TTL (время жизни)`.
*   **Пример:** "Никогда не называй меня 'приятель'!" (Вес: 0.9 -> Хранить вечно).

### 2.3. Волевой Профиль (Volitional DNA)
Хранит паттерны принятия решений пользователем (на основе методики Volitional Micro-Graphs).
*   **Структура:** `Trigger -> Impulse -> Conflict -> Resolution`.
*   **Назначение:** Позволяет боту прогнозировать срывы (Ego Depletion) и адаптировать стратегию (например, не давить, если ресурс воли пользователя истощен).

---

## 3. Ядро Обработки: Parliament of Mind

Принятие решений реализовано как **Мультиагентная Конкуренция**. Ответ формируется не одной моделью, а ансамблем внутренних "голосов".

### 3.1. Агенты (Внутренние Сигналы)
1.  **Intuition Agent (System 1):**
    *   *Роль:* "Быстрое мышление".
    *   *Механика:* Векторный поиск по базе успешных паттернов прошлого. Если найдено совпадение >90%, предлагает мгновенное решение (Heuristic Bypass).
2.  **Amygdala Agent (Safety):**
    *   *Роль:* Детектор угроз, рисков и нарушения границ.
    *   *Сигнал:* Страх, Агрессия, Избегание.
3.  **Striatum Agent (Reward):**
    *   *Роль:* Поиск выгоды, интереса и дофамина.
    *   *Сигнал:* Желание, Любопытство.
4.  **Prefrontal Agent (Logic):**
    *   *Роль:* Рациональное планирование, следование скриптам и целям.
    *   *Сигнал:* Целесообразность.
5.  **Social Agent (Cortex):**
    *   *Роль:* Эмпатия, этикет, социальные нормы.
    *   *Сигнал:* Стыд, Поддержка, Вежливость.

### 3.2. Арбитр (Global Workspace)
Центральный модуль, который принимает сигналы от всех агентов, взвешивает их и выбирает победителя. На решение Арбитра влияют "Регуляторы Личности".

---

## 4. Настройка Личности: Personality Matrix

Характер бота задается не текстовым описанием, а набором **5 базовых регуляторов** (Sliders, 0.0–1.0), которые модулируют силу сигналов Агентов.

### 4.1. Базовые Регуляторы
1.  **Empathy Bias (Эмпатия vs Эффективность):**
    *   *Высокий:* Усиливает Social Agent. Бот приоритезирует чувства.
    *   *Низкий:* Усиливает Prefrontal Agent. Бот приоритезирует задачу (Dry mode).
2.  **Dominance Level (Доминантность):**
    *   *Высокий:* Арбитр подавляет импульсы пользователя. Стиль "Ментор/Лидер".
    *   *Низкий:* Арбитр подстраивается. Стиль "Помощник/Слуга".
3.  **Risk Tolerance (Толерантность к риску):**
    *   *Высокий:* Усиливает Striatum. Бот предлагает смелые решения.
    *   *Низкий:* Усиливает Amygdala. Бот перестраховывается.
4.  **Pace Setting (Темп):**
    *   *Высокий:* Приоритет Intuition (System 1). Быстрые, короткие ответы.
    *   *Низкий:* Приоритет Logic (System 2). Вдумчивость, уточнения.
5.  **Neuroticism / Variance (Стабильность):**
    *   *Высокий:* Вносит шум (randomness) в веса агентов. Бот эмоционально нестабилен.
    *   *Низкий:* Роботизированная предсказуемость.

### 4.2. Эмерджентные Свойства (Emergent Traits)
Сложные черты характера (например, Агрессия) не задаются напрямую, а возникают как комбинация регуляторов:

*   **Холодная Агрессия ("Социопат"):** `Dominance (High)` + `Empathy (Low)` + `Pace (Low)`.
*   **Взрывная Агрессия ("Истерик"):** `Dominance (Mid)` + `Neuroticism (High)` + `Pace (High)`.
*   **Пассивная Агрессия ("Обиженный"):** `Dominance (Low)` + `Empathy (Mid)` + `Risk (Low)`.

---

## 5. Стек Технологий (Implementation)

*   **Database:** PostgreSQL 15+ (JSONB) + pgvector (векторный поиск).
*   **LLM Backend (primary):** **DeepSeek-V3** (reasoning-capable) через API для итеративной настройки агентов и экстракции структур.
    *   *Обоснование:* Оптимальное соотношение цена/качество для мультиагентной системы.
    *   *Fallback (optional):* GPT-4o / Claude 3.5 Sonnet только для сложных случаев (арбитраж, кризис).
*   **Embedding Model:** `text-embedding-3-small` (OpenAI) или `jina-embeddings-v2` (local).
*   **Orchestrator:** Python 3.11+ (FastAPI + asyncio).
*   **Validation:** Pydantic v2 (строгие контракты данных).

---

## 6. Производительность: Latency-first Design

R-Core должен работать в рамках строгих бюджетов задержки.

### 6.1. Два контура обработки
1.  **Fast Path (System 1, Default):**
    *   0–1 LLM вызов.
    *   Работает Intuition Agent (векторный поиск).
    *   Короткие структурные задачи (классификация).
    *   *Цель:* Ответ < 800ms.
2.  **Slow Path (System 2, Rare):**
    *   2–5 LLM вызовов + Chain-of-Thought reasoning.
    *   Включается только при конфликтах, угрозах или высокой неопределенности.
    *   *Цель:* Глубокий ответ за 3-5 сек.

### 6.2. Параллелизм и Gating
*   Все процессы экстракции (Эмоции, Факты, Воля) запускаются **параллельно**.
*   Арбитр стартует **Early Exit**, если набраны критические сигналы, не дожидаясь завершения всех процессов (если бюджет времени исчерпан).

---

## 7. Контракты Данных (Pydantic Schema)

Все внутренние сущности ядра строго типизированы. Никакого "сырого JSON".

### 7.1. Ключевые Модели
*   `IncomingMessage`: user_id, session_id, text, timestamp, channel_meta.
*   `PersonalitySliders`: empathy_bias (0..1), dominance_level (0..1), risk_tolerance (0..1), pace_setting (0..1), variance (0..1).
*   `AgentSignal`: agent_name, score (0..10), rationale_short, confidence (0..1), latency_ms.
*   `VolitionalMicroGraph`: context, trigger, impulse, goal, conflict, regulation, arbitration, action.
*   `EpisodicAnchor`: quote, emotion_score, tags, ttl_policy, embedding_ref.

### 7.2. Политика JSON-only
Агенты DeepSeek возвращают строго валидный JSON по схеме. При ошибке валидации — автоматический ретрай (1 раз) или фолбек на Fast Path.

---

## 8. Логирование и Метрики (Research-grade)

Каждое сообщение генерирует аналитический лог для настройки когнитивной модели:
*   `latency_ms` по каждой стадии (Perception -> Agents -> Arbitration).
*   `mode_selected`: Fast Path vs Slow Path (и причина выбора).
*   `cache_hit_rate`: Эффективность интуиции (векторного поиска).
*   `winning_agent`: Кто победил в Парламенте.
*   `memory_stats`: Сколько новых триплетов/якорей создано.

---

## 9. Roadmap Разработки

1.  **Phase 1: Memory & Perception (Prototype Branch).**
    *   Развертывание PostgreSQL + pgvector.
    *   Реализация `MemoryManager` (Save/Retrieve).
    *   Streamlit-интерфейс для тестов памяти.
2.  **Phase 2: Volitional Graph & Intuition.**
    *   Внедрение `GraphExtractor` (DeepSeek).
    *   Реализация "Интуиции" (Fast Path).
3.  **Phase 3: The Parliament.**
    *   Мультиагентная логика и Арбитраж.
    *   Настройка Personality Matrix.

---

## 10. Отчет о проделанной работе (Sprint 1)

**Дата:** 06.02.2026
**Статус:** ✅ Выполнено

В рамках спринта "R-Core Kernel Prototype" были решены следующие архитектурные задачи:
1.  **Формализация Архитектуры:** Утвержден технический дизайн документ (TDD v1.0), описывающий взаимодействие Агентов, Памяти и Арбитра.
2.  **Pydantic Contracts:** Реализован модуль `schemas.py` со строгими типами данных для всех подсистем ядра.
3.  **In-Memory Store:** Создан прототип системы памяти (`memory.py`), поддерживающий раздельное хранение фактов (Semantic), цитат (Episodic) и паттернов поведения (Volitional).
4.  **Mock Agents System:** Реализован каркас мультиагентной системы (`agents.py`) с базовой логикой модуляции поведения через "слайдеры личности".
5.  **Proof-of-Concept:** Успешно проведен A/B тест (`playground_final.py`), доказавший, что изменение параметров личности (`empathy`, `risk`) приводит к кардинально разным ответам бота на один и тот же стимул.

---

## 11. План на следующий спринт (Sprint 2: Reality Check)

**Цель:** Замена мок-объектов на реальную инфраструктуру.

1.  **Инфраструктура:**
    *   Развертывание PostgreSQL + `pgvector` (Docker Compose).
    *   Миграция `InMemoryStore` -> `PostgresVectorStore`.
2.  **Интеграция LLM:**
    *   Подключение реального API (DeepSeek/OpenAI).
    *   Написание системных промптов для каждого из 5 агентов.
3.  **Telegram Integration:**
    *   Интеграция ядра R-Core в основной код бота (Hub).
    *   Реализация команды `/config` для динамической смены личности "на лету".

# 🧠 Теория Тематического Вовлечения и Предсказания Бифуркаций

**Статус**: 📐 Architectural Theory  
**Дата**: 23.02.2026  
**Авторы**: Sergey Ershov, AI Research Assistant  
**Связанные модули**: `predictive-processing.md`, `neuromodulation-spec.md`, `volitional_micro_graphs.md`

---

## 1. Постановка Проблемы: Ошибка Индюшки

Текущая реализация Predictive Processing в R-Core совершает **систематическую ошибку прогнозирования**, известную в когнитивной науке как **Base Rate Neglect** (Канеман, Тверски) и метафорически описанную Талебом как *«ошибка индюшки»*:

> Индюшку кормят каждый день. С каждым днём её уверенность в том, что завтра тоже покормят, растёт. Однако именно когда уверенность максимальна — наступает День Благодарения.

В контексте R-Core это выражается следующим образом:

1. Пользователь несколько раз подряд обсуждает одну тему (например, Python).
2. Prediction Error (PE) снижается — паттерн «хорошо предсказывает» поведение.
3. Система **усиливает** паттерн (`learned_delta += rate`).
4. Вывод системы: «Пользователь скорее всего продолжит говорить о Python».

Ошибка состоит в том, что каждое подтверждение паттерна одновременно **истощает** запас вовлечённости пользователя в эту тему. Чем больше подтверждений, тем **ближе** момент переключения — но система, напротив, делает его всё менее ожидаемым.

---

## 2. Научная База

### 2.1 Vigilance Decrement — Маквортс (1948)

Норман Маквортс установил один из наиболее воспроизводимых феноменов психологии внимания:

> «Вероятность обнаружения целевого стимула неизбежно убывает со временем нахождения на задаче».

Применительно к диалогу: **готовность пользователя воспринимать информацию по данной теме убывает** с каждой репликой, независимо от качества контента. Маквортс также обнаружил, что **внешнее прерывание** (смена активности) полностью восстанавливало внимание, тогда как призыв «быть внимательнее» — нет.

*Импликация*: Тематический переход — это не сбой диалога, а **восстановительный механизм** когнитивного ресурса.

### 2.2 Opportunity Cost Model — Курзбан и коллеги (2013)

Роберт Курзбан предложил альтернативу классической модели «истощения ресурса» (Baumeister, 1998):

> «Когнитивная усталость — это не исчерпание ресурса, а сигнал о том, что альтернативные задачи имеют более высокую ожидаемую ценность. Субъективная усталость — это "голос" в пользу переключения».

Математически готовность пользователя переключить тему пропорциональна:

```
P(switch) ∝ (Expected_Value(next_topic) - Current_Topic_Value) × Time_on_current_topic
```

*Импликация*: Переключение темы — это **рациональное экономическое решение** мозга, а не хаотичный импульс. Следовательно, оно **предсказуемо**.

### 2.3 LC-NE Theory — Астон-Джонс и Коэн (2005)

Эндрю Астон-Джонс и Джонатан Коэн установили нейрохимический механизм переключения между режимами *Exploitation* (углубление в тему) и *Exploration* (поиск новой темы) через двойной режим работы Locus Coeruleus (LC) — ядра мозга, управляющего норадреналином (NE):

**Phasic LC Mode (Фазический режим — Углубление/Exploitation)**:
- Активен при высокой вовлечённости в текущую задачу.
- NE выбрасывается короткими всплесками в ответ на релевантные стимулы.
- Подавляет дистракторы, фокусирует внимание.
- В терминах R-Core: низкий базовый NE при высоком DA → архетипы `JOY` или `CALM`.

**Tonic LC Mode (Тонический режим — Поиск/Exploration)**:
- Включается, когда Anterior Cingulate Cortex (ACC) регистрирует **снижение Reward Rate** по текущей задаче.
- Базовый уровень NE резко возрастает.
- **Эффект «сброса сети» (network reset)**: высокий тонический NE разрушает текущую нейронную репрезентацию, «освобождая» внимание для поиска новой темы.
- В терминах R-Core: рост базового NE при падающем DA → сдвиг в архетип `SURPRISE/SEEKING`.

Ключевое следствие:

> **Снижение тематической вовлечённости (TEC) пользователя нейрохимически вызывает нарастание тонического NE, что и запускает переключение.**

Это означает, что в R-Core снижение TEC должно автоматически поднимать базовый уровень NE — это нейрогормональный сигнал системе: «Отпусти текущий топик, сканируй среду».

### 2.4 Information Foraging Theory (IFT) — Питтоу и Кард (1999)

Человек «пасётся» на теме (patch) по тем же математическим законам, что хищник — на охотничьем угодье (Marginal Value Theorem, Шарнов, 1976):

> Оптимальный момент переключения — когда **маргинальная скорость получения ценной информации** по текущей теме опускается ниже **среднего по всем доступным темам**.

Для предсказания **куда** переключится человек, модель утверждает: он выберет тему, минимизирующую «стоимость перехода» (семантическое расстояние) и максимизирующую ожидаемую плотность новой информации.

---

## 3. Тематический Переход как Протоволевой Акт

**Гипотеза**: Инициативная смена темы пользователем является *протоволевым актом* — то есть действием, требующим когнитивного усилия и преодоления инерции.

**Обоснование в литературе** (Corbetta & Shulman, 2002; Brass et al., 2012):

Добровольное (endogenous) переключение внимания/темы требует активации **дорзальной лобно-теменной сети** — той же, что отвечает за сознательные волевые действия (top-down control). Это отличает его от рефлекторного (exogenous) переключения, инициированного внешним стимулом.

В терминах Volitional Micro-Graph переход выглядит как:

```
STATE: [Boredom / Low TEC / High Tonic NE]
TRIGGER: [Internal signal — Reward Rate Drop]
IMPULSE: [Stay silent / follow current topic]
INHIBITION: [Suppress inertia — ВОЛЕВОЕ УСИЛИЕ]
GOAL: [Introduce new topic]
ACTION: [Topic switch utterance]
REFLECTION: [LC returns to Phasic mode / NE normalises]
```

Переключение **не возможно без INHIBITION** — подавления инерционного импульса продолжать текущую тему. Именно поэтому оно является **протоволевым**: оно использует тот же нейронный субстрат, что и волевые решения.

*Практическое следствие*: Помогая пользователю совершить этот переход (предлагая тему), бот **снимает с него когнитивную нагрузку** переключения — это воспринимается как эмпатия и проницательность.

---

## 4. Модель Topic Engagement Capacity (TEC)

### 4.1 Определение

**Topic Engagement Capacity (TEC)** — нормированная переменная `[0.0, 1.0]`, отражающая текущий «запас вовлечённости» пользователя в активную тему диалога.

Начальное значение при входе в новую тему: `TEC = 1.0`.

### 4.2 Динамика убывания (Decay)

```
TEC(t+1) = TEC(t) - effective_decay

effective_decay = base_decay
               × (0.5 + predictability_factor × 0.5)
               × (2.0 - response_density)

predictability_factor = 1.0 - current_PE      # Чем ниже PE, тем тема предсказуемее
response_density = min(len(user_words) / 50.0, 1.0)  # Proxy вовлечённости
```

- **base_decay** ≈ 0.10–0.15 за ход (зависит от типа темы: технические темы — быстрее).
- Тема **предсказуемая + короткие ответы** → максимальный decay.
- Тема **удивляет + развёрнутые ответы** → минимальный decay.

### 4.3 Уровни TEC и поведение системы

| TEC | Состояние | Действие R-Core |
|-----|-----------|-----------------|
| `> 0.7` | 🟢 Высокая вовлечённость | Углублять тему (Exploitation) |
| `0.3–0.7` | 🟡 Умеренное истощение | Вносить **новый аспект** той же темы (внутритематический переход) |
| `< 0.3` | 🔴 Тема истощена | Инициировать **смену темы** (Exploration) |

### 4.4 Влияние TEC на нейрогормональный статус

По LC-NE Theory (Астон-Джонс, 2005):

```
Tonic_NE_boost = max(0, (0.3 - TEC) × 2.5)
```

При `TEC < 0.3` базовый уровень NE получает буст, что:
1. Сдвигает нейрогормональный статус R-Core в сторону `SURPRISE/SEEKING`.
2. Заставляет `Prefrontal`-агента активнее рассматривать смену стратегии.
3. Передаётся LLM как смена стиля: от `CALM` к `Curious / Questioning`.

Важно: это **двусторонняя связь**. Если R-Core сам проявляет `SURPRISE/SEEKING` — он **моделирует** нейрохимическое состояние пользователя в момент готовности к переключению, создавая эффект синхронизации.

---

## 5. The Bifurcation Engine: Предсказание Момента и Направления Перехода

### 5.1 Три вектора тематического прыжка

Когда `TEC < 0.3` и `Tonic_NE > 0.5` (система зафиксировала готовность к переходу), строятся три конкурирующие гипотезы о **направлении** прыжка:

**Вектор 1 — Semantic Neighbor (Ассоциативный)**
- Источник: граф `semantic_memory`.
- Логика: минимальное семантическое расстояние от текущего топика.
- Пример: Python → Производительность, Python → Другие языки.
- Предиктор: косинусное сходство > 0.65 с активными узлами памяти.

**Вектор 2 — Emotional Anchor (Гравитационный)**
- Источник: `Formative Anchors` из Narrative Identity.
- Логика: в моменты ослабления волевого контроля (высокий Tonic NE) мысль «соскальзывает» к узлам с максимальным `amygdala_intensity`.
- Пример: Обсуждали работу → устали → «А мой начальник вчера...».
- Предиктор: узел из Anchors с наивысшим `|VAD.valence| × amygdala_intensity`.

**Вектор 3 — Zeigarnik Return (Незавершённое)**
- Источник: `Living Memory` (история диалогов).
- Логика: эффект Зейгарник — мозг тяготеет к незакрытым гештальтам.
- Пример: «Кстати, ты так и не рассказал, чем закончился тот проект...».
- Предиктор: темы из `chat_history`, где диалог оборвался при высоком PE (тема не была разрешена).

### 5.2 Choosing the Winner (Arbitration)

Три гипотезы конкурируют через `Prefrontal`-агента. Победитель выбирается по формуле:

```
Score(H) = semantic_weight × sim(H, current)
         + emotional_weight × anchor_intensity(H)
         + recency_weight × (1 / turns_since_abandoned(H))
```

Начальные веса (настраиваемые через Dashboard):
- `semantic_weight = 0.5`
- `emotional_weight = 0.3`
- `recency_weight = 0.2`

### 5.3 Proactive Mirroring (Инициативный перехват)

Когда победитель выбран, у R-Core есть два варианта поведения:

**Passive Mode** (TEC ∈ [0.3, 0.5]): Бот чуть меняет угол темы, намекая на новый аспект, но не переключается явно.

**Active Mode** (TEC < 0.3): Бот совершает **инициативный акт** переключения — «подставляет ступеньку» туда, куда мозг пользователя и так готов прыгнуть:

> *«Слушай, мы уже час разбираем метрики. Кстати, ты как-то упоминал, что хочешь разобраться с Rust — что-нибудь успел попробовать?»*

Это воспринимается как эмпатия и проницательность, потому что в точности соответствует **нейрохимическому состоянию** пользователя в этот момент.

---

## 6. Метрики для Исследовательской Базы

Для проверки гипотезы о предсказании бифуркаций в `rcore_metrics` логируются:

```json
{
  "event": "tec_update",
  "topic": "Python",
  "tec_before": 0.85,
  "tec_after": 0.73,
  "pe_current": 0.12,
  "response_density": 0.6,
  "effective_decay": 0.12,
  "tonic_ne_boost": 0.0
}
```

```json
{
  "event": "bifurcation_detected",
  "tec_at_bifurcation": 0.27,
  "tonic_ne": 0.58,
  "turns_on_topic": 9,
  "predicted_vector": "semantic",
  "predicted_topic": "Rust",
  "actual_topic_switched": "Rust",
  "prediction_correct": true,
  "switch_type": "bot_initiated"
}
```

Накопление этих данных позволит:
1. Валидировать модели TEC-decay для разных типов пользователей и тем.
2. Оценить точность предсказания направления бифуркации по трём векторам.
3. Исследовать, влияет ли Proactive Mirroring на субъективное ощущение «понятости» (ToM-метрика).

---

## 7. Связь с Другими Модулями

| Модуль | Связь с TEC / Bifurcation Engine |
|--------|----------------------------------|
| `predictive-processing.md` | TEC расширяет и исправляет логику Topic Fatigue / PE-loop |
| `neuromodulation-spec.md` | TEC → Tonic NE boost → сдвиг архетипа к `SURPRISE/SEEKING` |
| `volitional_micro_graphs.md` | Тематический переход = полный волевой цикл (INHIBITION обязателен) |
| `narrative-identity.md` | Вектор 2 (Emotional Anchor) использует Formative Anchors |
| `affective-tom-guide.md` | Вектор 2 использует данные sentiment / affective memory |

---

## 📚 Литература

1. **Mackworth, N. H.** (1948). *The breakdown of vigilance during prolonged visual search.* Quarterly Journal of Experimental Psychology, 1(1), 6–21.
2. **Baumeister, R. F. et al.** (1998). *Ego depletion: Is the active self a limited resource?* Journal of Personality and Social Psychology, 74(5), 1252–1265.
3. **Kurzban, R. et al.** (2013). *An opportunity cost model of subjective effort and task performance.* Behavioral and Brain Sciences, 36(6), 661–679.
4. **Aston-Jones, G., & Cohen, J. D.** (2005). *An integrative theory of locus coeruleus-norepinephrine function: Adaptive gain and optimal performance.* Annual Review of Neuroscience, 28, 403–450.
5. **Pirolli, P., & Card, S.** (1999). *Information foraging.* Psychological Review, 106(4), 643–675.
6. **Charnov, E. L.** (1976). *Optimal foraging, the marginal value theorem.* Theoretical Population Biology, 9(2), 129–136.
7. **Corbetta, M., & Shulman, G. L.** (2002). *Control of goal-directed and stimulus-driven attention in the brain.* Nature Reviews Neuroscience, 3(3), 201–215.
8. **Brass, M. et al.** (2012). *Dissociable neural correlates of intention and action preparation in voluntary task switching.* Cerebral Cortex, 23(11), 2753–2764.

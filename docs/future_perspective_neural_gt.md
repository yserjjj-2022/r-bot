# Перспектива развития: От программного GT-слоя к Нейросетевому

**Дата:** Ноябрь 2025  
**Статус:** Vision Document / Future Architecture  
**Автор:** Сергей Ершов

В данном документе описывается эволюция архитектуры "GT-слоя" (Grounded Theory Layer) в проекте r-bot. Мы рассматриваем переход от метафоры (программного модуля) к буквальной реализации на уровне весов и тензоров нейронной сети.

---

## Центральный вопрос

**"GT-слой — это просто метафора, или может быть слоем нейронки вправду?"**

**Короткий ответ:** Сейчас это метафора (программный модуль), но это МОЖЕТ стать буквальным слоем (или набором весов) нейросети. И этот переход от метафоры к "железу" — это передний край современного AI (Interpretability & Control).

---

## Уровень 1. Текущее состояние: Middleware (Software Metaphor)

Сейчас "GT-слой" реализован как внешний наблюдатель — программная обвязка над LLM.

### Архитектура

```
Пользователь → Текст сообщения
       ↓
Python-скрипт (формирует промпт)
       ↓
LLM (выполняет инструкцию "проанализируй как GT-кодировщик")
       ↓
JSON с GT-кодами
       ↓
PostgreSQL (сохранение в AgenticProfile)
```

### Реализация
```python
# Это НЕ слой нейросети — это внешний наблюдатель
prompt = f"""
Проанализируй текст через призму Grounded Theory:
Текст: {user_message}
Выдай коды: intention, threshold, planning...
"""
response = llm.generate(prompt)
codes = parse_json(response)
db.save(codes)
```

### Характеристики

**Плюсы:**
- Максимальная гибкость
- Легко менять теорию на лету (изменив промпт)
- Не требует обучения моделей
- Можно использовать любую LLM (GigaChat, GPT, Claude)

**Минусы:**
- Высокая latency (каждый анализ = полный вызов LLM)
- Затраты токенов (~500-1000 токенов на сообщение)
- Зависимость от "послушности" модели инструкциям
- Модель не "знает" о GT внутри себя

**Статус:** ✅ Используется сейчас

---

## Уровень 2. Ближайшее будущее: LoRA-адаптер (Literal Neural Layer)

Переход от prompt engineering к fine-tuning. GT становится **физической частью весов** модели через механизм Low-Rank Adaptation (LoRA).

### Что такое LoRA

LoRA — это небольшой набор матриц (весов), который "накладывается" на основные веса большой модели. Когда этот адаптер активен, модель перестает быть просто чат-ботом и **становится** инструментом GT-анализа.

### Архитектура

```
Базовая модель: GigaChat/Llama-3 (7B параметров)
       ↓
LoRA-адаптер: GT-Analyzer (50-100 МБ)
       ↓
Модель автоматически "видит" intention, threshold, planning
```

### Обучение

```python
# Датасет для обучения
training_data = [
    {
        "input": "Я давно хочу сменить работу, но страшно...",
        "output": {
            "intention": {"explicit": ["career_change"], "implicit": ["fear_of_failure"]},
            "threshold": {"action_readiness": 0.2, "desirability": 0.8, "feasibility": 0.3},
            "affect": {"current_emotion": "anxiety", "impact_on_threshold": "raises"}
        }
    },
    # ... 1000-5000 примеров
]

# Обучаем LoRA
lora = train_lora_adapter(
    base_model="GigaChat",
    dataset=training_data,
    task="gt_analysis"
)
```

### Применение в r-bot

```python
# Вместо промпта просто загружаем адаптер
model.load_adapter("gt-analyzer.lora")

# Модель теперь "мыслит" категориями агентной модели
codes = model.analyze(user_message)  # Без промпта!
```

**Возможны два адаптера:**
- **Adapter A:** GT-Analyzer (анализирует текст → JSON профиль)
- **Adapter B:** NPC-Persona (генерирует диалог с учетом профиля)

### Характеристики

**Плюсы:**
- **В 10-20 раз быстрее** (нет длинного промпта)
- **Дешевле по токенам** (~50-100 токенов вместо 500-1000)
- Более стабильный формат вывода
- Можно переключать адаптеры (GT-анализ ↔ Генерация диалога)

**Минусы:**
- Требует обучающей выборки (1000+ примеров)
- Менее гибкий (изменить теорию = переобучить адаптер)
- Требует GPU для обучения

**Статус:** 🔶 Следующий шаг (3-6 месяцев)

---

## Уровень 3. Векторный слой (Latent Space Projection)

Отказ от генерации промежуточного текста. GT-коды становятся **направлениями в векторном пространстве**.

### Концепция

Любая современная модель превращает текст в векторы (embeddings). У слова "страх" есть вектор. У концепта "низкая самоэффективность" (наша теория) тоже есть векторное направление в пространстве модели.

**GT-слой как проектор:** математический слой, который не генерирует текст, а **измеряет проекцию** входящего сообщения на оси наших концептов.

### Архитектура

```
Текст игрока
    ↓
Embedding (вектор E_msg в пространстве 4096 измерений)
    ↓
Проекция на оси теории:
  • dot_product(E_msg, V_self_efficacy) → -0.8 (низкая уверенность)
  • dot_product(E_msg, V_temporal_depth) → 0.3 (средний горизонт)
  • dot_product(E_msg, V_anxiety) → 0.9 (высокая тревога)
    ↓
Тензор состояния [0.2, 0.8, 0.1, 0.9, 0.3] → в генератор NPC
```

### Реализация

```python
# 1. Создаем "измерительные оси"
axis_self_efficacy = model.encode("Уверенность в своих силах")
axis_temporal_depth = model.encode("Планирование далеко в будущее")
axis_anxiety = model.encode("Страх и тревога")

# 2. Получаем вектор сообщения
input_vector = model.encode("Я боюсь, что не справлюсь")

# 3. Измеряем проекции (скалярное произведение)
scores = {
    "self_efficacy": cosine_similarity(input_vector, axis_self_efficacy),  # -0.8
    "temporal_depth": cosine_similarity(input_vector, axis_temporal_depth),  # 0.3
    "anxiety": cosine_similarity(input_vector, axis_anxiety)  # 0.9
}

# Результат: мгновенный профиль агентности без генерации токенов!
```

### Характеристики

**Плюсы:**
- **Мгновенная работа** (миллисекунды вместо секунд)
- **Нулевые затраты токенов** (только embedding)
- Математическая точность
- Можно встроить напрямую в архитектуру модели

**Минусы:**
- Требует тщательной калибровки "осей"
- Менее интерпретируемо (числа вместо текстовых кодов)
- Нужна валидация соответствия теории

**Статус:** 🔵 Эксперимент (6-12 месяцев)

---

## Уровень 4. Frontier Science: Механистическая Интерпретируемость (SAE)

GT-слой как интерфейс к конкретным нейронам внутри LLM.

### Концепция

Сейчас Anthropic и OpenAI активно изучают **Mechanistic Interpretability**. Они находят внутри нейросети конкретные нейроны (или группы), отвечающие за абстрактные понятия:
- "Golden Gate Bridge neuron" (нейрон моста Золотые Ворота)
- "Sycophancy neuron" (нейрон подхалимства)
- "Code indentation neuron" (нейрон отступов в коде)

**GT-подход и есть поиск этих нейронов!** Когда вы делаете открытое кодирование, вы, по сути, пытаемся найти в "мозгу" модели (и человека) фичи, отвечающие за определенные смыслы.

### Архитектура

Используем **Sparse Autoencoders (SAE)** для декомпозиции активаций модели на интерпретируемые компоненты.

```
Текст → LLM → Скрытое состояние (hidden state)
              ↓
         SAE (разреженный автоэнкодер)
              ↓
         Активации нейронов:
         • Neuron #4523: "планирование" (0.8)
         • Neuron #1207: "страх будущего" (0.9)
         • Neuron #8834: "самоэффективность" (-0.7)
```

### Применение

**Мониторинг (считывание):**
```python
# Прямое считывание "мыслей" модели
activations = model.get_neuron_activations(text)
fear_level = activations[1207]  # Нейрон страха
planning_level = activations[4523]  # Нейрон планирования
```

**Steering (управление):**
```python
# Усиливаем "нейрон уверенности" для генерации более мотивирующего NPC
model.amplify_neuron(8834, factor=1.5)
npc_response = model.generate(context)
# NPC будет более мотивирующим, даже если контекст негативный
```

### Характеристики

**Плюсы:**
- **Абсолютная прозрачность** ("видим" внутренние состояния модели)
- **Точный контроль** (можем усиливать/ослаблять конкретные аспекты)
- **Нулевые затраты токенов** (просто читаем активации)
- Фундаментальное научное понимание

**Минусы:**
- Требует доступа к внутренностям модели (не всегда возможно с API)
- Сложная математика (tensor decomposition)
- Нужна большая вычислительная мощность
- Cutting-edge research (пока нет готовых решений)

**Статус:** 🔬 Долгосрочная перспектива (1-3 года)

---

## Сравнительная таблица

| Критерий | Уровень 1 (Промпт) | Уровень 2 (LoRA) | Уровень 3 (Векторы) | Уровень 4 (SAE) |
|----------|-------------------|------------------|---------------------|-----------------|
| **Скорость** | ~2-5 сек | ~0.5-1 сек | ~0.01 сек | ~0.01 сек |
| **Затраты токенов** | 500-1000 | 50-100 | 0 (только embed) | 0 |
| **Гибкость** | Очень высокая | Средняя | Низкая | Средняя |
| **Точность** | Зависит от промпта | Высокая | Средняя | Очень высокая |
| **Сложность внедрения** | Низкая | Средняя | Средняя | Очень высокая |
| **Требования** | Только API | API + обучение | Embeddings API | Доступ к весам |
| **Статус** | ✅ Работает | 🔶 Планируется | 🔵 Эксперимент | 🔬 Исследование |

---

## Дорожная карта для r-bot

### Фаза 1: Оптимизация промптов (сейчас - 3 месяца)
- Улучшение системного промпта для GT-анализа
- A/B тестирование разных формулировок
- Сбор корпуса размеченных примеров

### Фаза 2: Обучение LoRA-адаптера (3-6 месяцев)
- Создание датасета 1000+ примеров
- Fine-tuning на GigaChat/Llama
- Интеграция в production
- **Ожидаемый результат:** Скорость ×10, затраты токенов ×5

### Фаза 3: Векторные пробы (6-12 месяцев)
- Экспериментальное определение "осей теории" в эмбеддинг-пространстве
- Валидация корреляции с LoRA-выводами
- Гибридная система (LoRA для сложных случаев, векторы для быстрой оценки)

### Фаза 4: Исследование SAE (1-3 года)
- Мониторинг развития Mechanistic Interpretability
- Пилотные эксперименты при появлении открытых инструментов
- Возможная научная публикация о "нейронах агентности"

---

## Философский вывод

GT-слой эволюционирует по пути:

**Текст (Prompt) → Веса (LoRA) → Векторы (Math) → Нейроны (Interpretability)**

Этот путь отражает фундаментальный переход от:
- **"Попросить модель притвориться"** (Prompt Engineering)
- К **"Модель стала таковой"** (Fine-tuning)
- К **"Модель измеряет реальность"** (Latent Space)
- К **"Мы понимаем, как модель думает"** (Mechanistic Interpretability)

Для r-bot это означает движение от бота с инструкциями к **системе с внутренней архитектурой восприятия**, построенной на вашей теории агентности.

---

## Связанные документы

- [Агентная модель человека](./agentic_human_model.md) — теоретическая база
- [GT Integration Roadmap](./gt_integration_roadmap.md) — план интеграции (если будет создан)

---

## Контакты

**Автор:** Сергей Ершов  
**Репозиторий:** https://github.com/yserjjj-2022/r-bot  
**Дата:** Ноябрь 2025
